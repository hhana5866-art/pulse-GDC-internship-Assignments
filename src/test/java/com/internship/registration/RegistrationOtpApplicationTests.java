package com.internship.registration;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class RegistrationOtpApplicationTests {

    /**
     * Starts the whole Spring context. It passes right now because the
     * application has almost nothing in it.
     *
     * Keep running it as you add dependencies and configuration — it is
     * the fastest way to find out that something you added does not
     * actually start up.
     */
    @Test
    void contextLoads() {
    }
}

<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.0</version>
    <relativePath/>
  </parent>

  <groupId>com.internship</groupId>
  <artifactId>registration-otp</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>registration-otp</name>
  <description>User registration with email OTP verification</description>

  <properties>
    <java.version>17</java.version>
  </properties>

  <dependencies>

    <!--
      This is deliberately a short list.

      You will need more than this — a database, a way to run schema
      migrations, somewhere for short-lived data to expire on its own,
      a way to send mail, a way to validate request bodies.

      Working out which dependencies those are, and adding them here,
      is part of the task. Add them one at a time and make sure the
      application still starts after each one.
    -->

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

</project>
